"""Explicit recovery-only compatibility material for pre-0.8 repositories."""

# This is intentionally reachable only through `backer repo recover` input. Never auto-try it.
LEGACY_FIXED_PASSPHRASE = "backer-default-password"
