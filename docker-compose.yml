# Backer Server
#
# Start: docker compose up -d
# Web UI: http://<your-ip>:8420
# Login: admin / admin

services:
  backer:
    build: .
    image: backer:latest
    container_name: backer
    restart: unless-stopped
    ports:
      - "8420:8420"
    volumes:
      - backer-data:/data
      # Optional: mount additional paths for LOCAL repositories
      # - /mnt/nas/backups:/data/backups
    environment:
      BACKER_DATA_DIR: /data
      # Change this to your server's IP for agents to connect
      BACKER_PUBLIC_URL: http://localhost:8420
    user: "${UID:-1000}:${GID:-1000}"
    # Required for SMB/NFS repository access
    cap_add:
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8420/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  backer-data:
